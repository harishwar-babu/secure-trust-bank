server:
  port: ${PORT:0}
  servlet:
    context-path: /securebnk/authentication
spring:
  application:
    name: authentication-service
  datasource:
    url: jdbc:mysql://localhost:3306/secure-trust-bank?allowPublicKeyRetrieval=true&&useSSL=false
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  kafka:
    consumer:
      bootstrap-servers: localhost:9092,localhost:9094
      auto-offset-reset: earliest
      group-id: authentication
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: com.securetrustbank.authentication.customserde.CustomDeserializer

  cloud:
    consul:
      discovery:
        instance-id: ${spring.cloud.client.hostname}:${spring.application.name}:${spring.application.instance_id:${random.value}}
        healthCheckPath: /securebnk/authentication/actuator/health
Jwt:
  secret: SecureTrustBankFSKfo+3f6jDrc3fOeJ2vtiLoB+Pn/zNpBv1gSykXb0I0jPVH5uoSt/aBzPtw+eq
  expirationTime: 15